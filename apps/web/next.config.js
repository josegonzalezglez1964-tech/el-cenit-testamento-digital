/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
                            ges: {
                              },
  async headers() {
    ret    ret    ret        source: '/:p    ret    ret    ret        source: '/:p    ret    ret    ret        source: '/:p    ret  key: 'X-Content-Type-Options', value: 'nosniff' },
          { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
        ],
      },
    ];
  },
};

module.exports = nextConfig;
